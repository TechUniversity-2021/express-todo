const { promisifyReadFile: readFile } = require('../utilities/fsFunctions.utilities');
const { parseFileData } = require('../utilities/parseFileData.utilities');

// const getAllTodo = async() => {
//     const await
// }

module.exports = {
  getAllTodo,
};
